# Tayla Cannon

## GitHub
- Account: **RebuildrApp**

## Infrastructure & Cloud
- **Cloud provider:** DigitalOcean for everything — databases, App Platform, compute, container registry
- **No Homebrew** — installs CLI tools manually to `~/.local/bin/`
- **OS:** macOS (Darwin)

## Tools & CLI
- **GitHub CLI:** `~/.local/bin/gh` — authenticated as RebuildrApp
- **DigitalOcean CLI:** `~/.local/bin/doctl` — auth via `doctl auth init`
- **Shell:** zsh

## Working Style
- Prefers direct, concise communication
- Wants Claude to just do the work, not over-explain
- Hands-on builder — gives Claude ambitious tasks and expects execution
